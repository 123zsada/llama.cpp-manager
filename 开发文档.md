# llama.cpp 管理器 — 开发文档

## 1. 项目概述

**项目名称**：llama.cpp Manager（暂定）
**技术栈**：Go + Wails v2 + Vue 3
**目标平台**：Windows x86_64（优先），后续可扩展 Linux/macOS
**核心功能**：图形化管理多个 llama.cpp 实例，支持多版本切换、可视化命令构建、进程生命周期管理。

## 2. 功能范围

### 2.1 版本管理
- 支持从 GitHub Releases 下载官方预构建二进制（Windows x86_64）
- 支持指向本地已有构建目录
- 每个版本独立存放，互不干扰
- 自动识别后端类型（CUDA / Vulkan / CPU / AVX2）
- 通过 `--version` 读取构建号并注册

### 2.2 实例管理
- 创建多个 llama.cpp 实例，每个实例绑定一个版本
- 实例操作：启动、停止、重启、删除
- 实例状态实时显示：未启动 / 启动中 / 运行中 / 已停止 / 异常退出
- 实例配置持久化（保存为 JSON 预设）

### 2.3 可视化命令构建
- 根据选中版本的 `--help` 输出动态生成参数面板
- 参数按功能分组：模型、GPU、上下文、采样、服务器、性能
- 每个参数提供说明 tooltip
- 实时命令预览
- 完整命令解释器（逐参数解释）

### 2.4 日志与监控
- 实时捕获 llama.cpp 的 stdout / stderr
- 日志按实例分离展示
- 简单异常检测（如 b10290 的 token 重复 bug）

## 3. 技术架构

### 3.1 目录结构

```
llama-manager/
├── main.go                    # Wails 入口
├── app.go                     # App 结构体与绑定方法
├── wails.json                 # Wails 配置
├── go.mod
├── internal/
│   ├── runtime/               # 版本管理
│   │   ├── runtime.go         # LlamaRuntime 定义
│   │   ├── download.go        # GitHub 下载逻辑
│   │   ├── scan.go            # 本地目录扫描
│   │   └── parse_help.go      # --help 解析
│   ├── instance/              # 实例管理
│   │   ├── instance.go        # LlamaInstance 定义
│   │   ├── manager.go         # 实例池管理
│   │   └── process.go         # 进程启停、日志捕获
│   ├── config/                # 配置持久化
│   │   └── store.go
│   └── cmd/                   # 命令构建与解释
│       ├── builder.go
│       └── explainer.go
├── frontend/
│   ├── src/
│   │   ├── App.vue
│   │   ├── main.js
│   │   ├── views/
│   │   │   ├── Runtimes.vue   # 版本管理页
│   │   │   ├── Instances.vue  # 实例列表页
│   │   │   ├── InstanceEdit.vue # 实例编辑（命令构建器）
│   │   │   └── Logs.vue       # 日志页
│   │   ├── components/
│   │   │   ├── ParamGroup.vue
│   │   │   ├── ParamItem.vue
│   │   │   ├── CommandPreview.vue
│   │   │   └── CommandExplainer.vue
│   │   ├── stores/            # Pinia
│   │   │   ├── runtimes.js
│   │   │   └── instances.js
│   │   └── api/               # Wails 绑定封装
│   └── package.json
└── docs/
    └── DEVELOPMENT.md         # 本文档
```

### 3.2 数据流

```
用户操作 (Vue)
    ↓
Wails Binding (Go 方法)
    ↓
internal/runtime | instance | cmd
    ↓
文件系统 / 进程 / 网络
    ↓
事件回推 (runtime.EventsEmit)
    ↓
前端状态更新 (Pinia)
```

## 4. 数据模型

### 4.1 LlamaRuntime（版本）

```go
type LlamaRuntime struct {
    ID         string `json:"id"`          // "b10290-cuda"
    BuildTag   string `json:"buildTag"`    // "b10290"
    Backend    string `json:"backend"`     // "cuda" / "vulkan" / "cpu" / "avx2"
    Arch       string `json:"arch"`        // "x64"
    Executable string `json:"executable"`  // llama-server.exe 绝对路径
    WorkDir    string `json:"workDir"`
    Source     string `json:"source"`      // "download" / "local"
    InstalledAt time.Time `json:"installedAt"`
}
```

### 4.2 LlamaInstance（实例）

```go
type LlamaInstance struct {
    ID         string            `json:"id"`
    Name       string            `json:"name"`
    RuntimeID  string            `json:"runtimeId"`
    ModelPath  string            `json:"modelPath"`
    Params     map[string]string `json:"params"`     // flag -> value
    ExtraArgs  []string          `json:"extraArgs"`  // 原始附加参数
    WorkDir    string            `json:"workDir"`
    AutoStart  bool              `json:"autoStart"`
    CreatedAt  time.Time         `json:"createdAt"`
}
```

### 4.3 运行时状态（内存）

```go
type InstanceState struct {
    InstanceID string
    Status     string    // "stopped" / "starting" / "running" / "error"
    PID        int
    Cmd        *exec.Cmd
    StartedAt  time.Time
    LastError  string
}
```

### 4.4 参数定义（从 --help 解析）

```go
type ParamDef struct {
    Flag      string `json:"flag"`       // "-m"
    Alias     string `json:"alias"`      // "--model"
    Desc      string `json:"desc"`
    HasValue  bool   `json:"hasValue"`   // 是否接受值
    Group     string `json:"group"`      // 功能分组
}
```

## 5. 后端核心模块

### 5.1 版本下载（internal/runtime/download.go）

```
GetAvailableReleases() []Release
    → 调用 GitHub API: /repos/ggml-org/llama.cpp/releases
    → 筛选 tag 匹配 bNNNN 格式
    → 返回前 N 个版本

DownloadRuntime(tag, backend string) (*LlamaRuntime, error)
    → 构造资产名: llama-<tag>-bin-win-<backend>-x64.zip
    → 下载到临时目录
    → 解压到 runtimes/<tag>-<backend>/
    → 注册到 runtimes.json
```

**注意**：GitHub API 有速率限制，未认证时 60 次/小时。建议结果缓存 1 小时。

### 5.2 本地扫描（internal/runtime/scan.go）

```
ScanLocalDir(dir string) (*LlamaRuntime, error)
    → 检查 dir 下是否存在 llama-server.exe
    → 执行 llama-server.exe --version 读取构建号
    → 解析 "version: 10290 (c8e03ce81)" 提取 b10290
```

### 5.3 --help 解析（internal/runtime/parse_help.go）

llama.cpp 的 `--help` 输出格式大致为：

```
-m, --model FNAME    model path to load
-ngl, --n-gpu-layers N  number of layers to store in VRAM
```

解析策略：
- 逐行扫描，匹配 `^\s*(-\w+),\s*(--[\w-]+)\s*(\S+)?\s+(.*)$`
- 提取 flag、alias、valuePlaceholder、desc
- 根据 desc 关键词或 flag 前缀进行分组

### 5.4 进程管理（internal/instance/process.go）

```go
func (m *Manager) Start(id string) error {
    // 1. 拼装命令
    args := cmd.Build(instance, runtime)
    // 2. 创建进程
    cmd := exec.CommandContext(ctx, runtime.Executable, args...)
    cmd.Dir = instance.WorkDir
    cmd.SysProcAttr = &syscall.SysProcAttr{
        CreationFlags: 0x00000008, // DETACHED_PROCESS
    }
    // 3. 重定向输出
    stdout, _ := cmd.StdoutPipe()
    stderr, _ := cmd.StderrPipe()
    go m.streamLogs(id, stdout, "stdout")
    go m.streamLogs(id, stderr, "stderr")
    // 4. 启动
    cmd.Start()
    // 5. 监控退出
    go func() {
        err := cmd.Wait()
        m.emitStatusChange(id, err)
    }()
}
```

**停止策略**：Windows 下先尝试 `cmd.Process.Kill()`，若无效则使用 `taskkill /F /T /PID` 确保杀掉子进程树。

### 5.5 命令构建（internal/cmd/builder.go）

```go
func Build(inst LlamaInstance, rt LlamaRuntime) []string {
    args := []string{}
    // 固定参数顺序，便于解释器解析
    if inst.ModelPath != "" {
        args = append(args, "-m", inst.ModelPath)
    }
    // 按 flag 字典序排列其余参数
    keys := sortedKeys(inst.Params)
    for _, k := range keys {
        v := inst.Params[k]
        args = append(args, k)
        if v != "" {
            args = append(args, v)
        }
    }
    args = append(args, inst.ExtraArgs...)
    return args
}
```

### 5.6 命令解释（internal/cmd/explainer.go）

```go
func Explain(args []string, defs []ParamDef) []Explanation {
    // 将 args 按 flag 边界切分
    // 每个 flag 在 defs 中查找对应的 ParamDef
    // 返回 {Flag, Value, Desc, Note}
}

type Explanation struct {
    Flag  string `json:"flag"`
    Value string `json:"value"`
    Desc  string `json:"desc"`
    Note  string `json:"note"` // 版本相关警告
}
```

## 6. Wails 绑定接口

在 `app.go` 中暴露给前端的核心方法：

```go
// 版本管理
func (a *App) ListRuntimes() []LlamaRuntime
func (a *App) FetchAvailableReleases() ([]Release, error)
func (a *App) DownloadRuntime(tag, backend string) (*LlamaRuntime, error)
func (a *App) AddLocalRuntime(dir string) (*LlamaRuntime, error)
func (a *App) RemoveRuntime(id string) error
func (a *App) GetRuntimeParams(id string) ([]ParamDef, error)

// 实例管理
func (a *App) ListInstances() []LlamaInstance
func (a *App) CreateInstance(inst LlamaInstance) (*LlamaInstance, error)
func (a *App) UpdateInstance(inst LlamaInstance) error
func (a *App) DeleteInstance(id string) error
func (a *App) StartInstance(id string) error
func (a *App) StopInstance(id string) error
func (a *App) RestartInstance(id string) error
func (a *App) GetInstanceState(id string) InstanceState

// 命令工具
func (a *App) PreviewCommand(inst LlamaInstance) string
func (a *App) ExplainCommand(inst LlamaInstance) ([]Explanation, error)

// 路径选择
func (a *App) SelectDirectory() (string, error)
func (a *App) SelectFile(filters string) (string, error)

// 日志
func (a *App) GetLogs(instanceID string) []string
func (a *App) ClearLogs(instanceID string)
```

**事件**（Go → Vue）：

```
instance:status   { id, status, pid, error }
instance:log      { id, line, stream }
runtime:progress  { tag, percent }   // 下载进度
```

## 7. 前端设计

### 7.1 页面结构

- **版本管理页**（Runtimes.vue）
  - 已安装版本列表（卡片式，显示构建号、后端、路径）
  - “添加版本”按钮 → 弹窗：从 GitHub 下载 / 选择本地目录
  - 下载时显示进度条
  - 每个版本可查看其参数表（`--help` 解析结果）

- **实例列表页**（Instances.vue）
  - 表格或卡片列表
  - 每行：名称、绑定版本、模型、状态灯、操作按钮（启动/停止/重启/编辑/删除）
  - 顶部“新建实例”按钮

- **实例编辑页**（InstanceEdit.vue）
  - 左侧：参数分组树（可折叠）
  - 中间：参数表单（根据分组展开）
  - 右侧：命令预览 + 解释器
  - 底部：保存 / 另存为预设 / 试运行

- **日志页**（Logs.vue）
  - 实例选择器
  - 实时日志滚动区
  - 过滤 / 搜索 / 清空

### 7.2 参数面板动态渲染

```vue
<template>
  <div v-for="group in paramGroups" :key="group.name">
    <h3 @click="group.expanded = !group.expanded">
      {{ group.name }} ({{ group.params.length }})
    </h3>
    <div v-show="group.expanded">
      <ParamItem
        v-for="p in group.params"
        :key="p.flag"
        :def="p"
        v-model="form.params[p.flag]"
      />
    </div>
  </div>
</template>
```

### 7.3 命令预览

```vue
<template>
  <pre class="command-preview">
    {{ runtime.Executable }} {{ previewArgs.join(' ') }}
  </pre>
</template>
```

参数变化时通过 `computed` 实时更新 `previewArgs`，无需请求后端。

### 7.4 命令解释器

```vue
<template>
  <div class="explainer">
    <div v-for="e in explanations" :key="e.flag" class="explain-item">
      <code>{{ e.flag }} {{ e.value }}</code>
      <span class="desc">{{ e.desc }}</span>
      <span v-if="e.note" class="note">{{ e.note }}</span>
    </div>
  </div>
</template>
```

## 8. 开发路线

### Phase 1：骨架与版本管理
- [ ] Wails 项目初始化，Vue 3 + Pinia 搭建
- [ ] `ListRuntimes` / `AddLocalRuntime` / `RemoveRuntime`
- [ ] 版本管理页 UI
- [ ] `--version` 与 `--help` 解析

### Phase 2：实例与进程
- [ ] 实例 CRUD + 持久化
- [ ] 进程启动/停止/重启
- [ ] 状态事件推送
- [ ] 实例列表页 UI

### Phase 3：命令构建器
- [ ] 动态参数面板
- [ ] 命令预览
- [ ] 命令解释器
- [ ] 实例编辑页 UI

### Phase 4：日志与下载
- [ ] 实时日志流
- [ ] GitHub Release 下载
- [ ] 下载进度事件
- [ ] 日志页 UI

### Phase 5：优化
- [ ] 异常检测（token 重复等）
- [ ] 预设管理
- [ ] 多语言
- [ ] 打包与自动更新

## 9. 关键技术风险

| 风险 | 影响 | 缓解方案 |
|------|------|---------|
| GitHub API 限流 | 无法获取版本列表 | 本地缓存 1 小时；支持手动输入 tag |
| `--help` 格式变更 | 参数解析失败 | 多正则兼容；解析失败时回退到内置参数表 |
| Windows 进程树 | 停止时残留子进程 | 使用 taskkill /T；记录 PID 树 |
| 旧版本 bug（如 b10290 token 重复） | 用户体验差 | 日志模式检测 + 版本警告 |
| 不同后端 DLL 依赖 | 启动失败 | 启动前检查同目录 DLL；给出明确错误提示 |
| 中文字符路径 | 命令行解析错误 | 统一使用 UTF-8，必要时用引号包裹路径 |

## 10. 附录

### 10.1 参考版本
- 当前开发验证版本：b10290 (c8e03ce81)，Clang 20.1.8，Windows x86_64
- 最新版本查询：https://github.com/ggml-org/llama.cpp/releases

### 10.2 资产命名规则（GitHub Releases）
```
llama-<tag>-bin-win-cuda-x64.zip
llama-<tag>-bin-win-vulkan-x64.zip
llama-<tag>-bin-win-cpu-x64.zip
llama-<tag>-bin-win-avx2-x64.zip
```

### 10.3 常用启动参数速查

| Flag | 说明 |
|------|------|
| `-m` | 模型文件路径 |
| `-ngl` | GPU 层数 |
| `-c` | 上下文大小 |
| `-t` | 线程数 |
| `--host` | 监听地址 |
| `--port` | 监听端口 |
| `-np` | 并行请求数 |
| `--no-mmap` | 禁用内存映射 |

---

文档版本：v0.1
最后更新：2026-09-12
适用项目阶段：初始化设计